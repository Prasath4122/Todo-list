
-- Queries use to create the table and their values using CRUD methods

-- CREATE TABLE items (
--   id SERIAL PRIMARY KEY,
--   title VARCHAR(100) NOT NULL
-- );

-- SELECT * FROM items ORDER BY id ASC;

-- INSERT INTO items (title) VALUES ($1), [item];

-- UPDATE items SET title = ($2) WHERE id = $1, [id, item];

-- DELETE FROM items WHERE id = $1, [id];